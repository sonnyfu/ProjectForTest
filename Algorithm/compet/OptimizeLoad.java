package compet;

import java.util.ArrayList;
import java.util.Arrays;

public class OptimizeLoad {
	/**例：5，a[5]
	 * 
	 * 输入：n个箱子载重w[i]，载重为W的船
	 * 处理:回溯法
	 * 1、排序，
	 * 2、依次放入最重的箱子i，判断有无超载，i++，若能够==W，则输出，
	 * 3、若大于W，则该i次放入后序较小的箱子，若剩余的都不能满足==W，则回溯上一步放入小于它的箱子，
	 * 循环2，3，有则返回。
	 * 4、最后的输出为m*n的时间复杂度
	 * 
	 * 输出：选择的
	 */
	
	
/*	static int[] maxLoad(int w[],int w){
		
		
		
	}*/
	
}
